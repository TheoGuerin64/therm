---
- name: Install dependencies and apply roles
  hosts: localhost
  connection: local

  vars:
    oh_my_zsh_plugins:
      - git

  roles:
    - fzf
    - ghostty
    - neovim
    - uv
    - vscode
    - zsh
