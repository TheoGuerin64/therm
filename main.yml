---
- name: Install dependencies and apply roles
  hosts: localhost
  connection: local

  vars:
    oh_my_zsh_plugins:
      - sudo

  roles:
    - rust
    - fzf
    - ghostty
    - git
    - golang
    - neovim
    - uv
    - vscode
    - zig
    - zsh
